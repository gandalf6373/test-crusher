---
name: test-crusher
description: "当你拿到已整理好的开发功能点列表（.md 或 .xlsx 格式），需要生成测试用例 Excel 时使用。"
---

# Test Crusher

当用户给你的是**已整理的开发功能点列表**，希望生成可直接审核、执行测试用例 Excel 时，使用这个 skill。

## 支持平台

- Claude Code ✅
- Codex ✅
- OpenClaw ✅
- QClaw ✅
- openCode ✅
- 其他支持 Skill 的工具 ✅

## 输入格式

支持两种输入格式：

### 1. Markdown 格式
从 "## 结构化任务（开发点）" 或 "## 开发功能点" 章节中提取，格式如：
```markdown
- [后端] 设备新增接口（device_no 唯一校验 / station_id 存在性校验 / 默认 AVAILABLE）
- [前端] 新增设备表单页（字段 / 必填校验 / 提交提示）
- [测试] 正常新增 / device_no 重复拦截 / station_id 无效拦截
```

### 2. Excel 格式 (.xlsx)
表格列：系统 / 模块 / 功能点 / 开发点 / 类型 / 描述 / 优先级 / 备注

## 你要产出什么

将输入的开发功能点列表，转换为**测试用例 Excel 文件**（.xlsx 格式）。

### Excel 输出字段

| 字段 | 说明 |
|------|------|
| 用例编号 | 格式：`模块简称-序号`（如 `DEV-001`），按模块分组编号 |
| 模块 | 对应开发功能点中的"模块" |
| 用例标题 | 简洁描述测试目标 |
| 优先级 | P0（核心功能）/ P1（重要功能）/ P2（一般功能），从输入提取或推断 |
| 用例类型 | 功能测试 / 接口测试 / 边界测试 / 异常测试 |
| 前置条件 | 执行测试前需要满足的条件 |
| 执行路径 | 调用接口或操作路径 |
| 输入数据 | 测试所需的输入参数 |
| 操作步骤 | 详细操作步骤（1. 2. 3.） |
| 预期结果 | 期望的返回或响应 |
| 执行方式 | 手动执行 / 自动化可执行 |
| 是否可自动化 | 是 / 否 / 待定 |
| 备注 | 补充说明 |

## 处理逻辑

### 用例编号规则

- 格式：`{模块简称}-{序号}`
- 模块简称：取模块名拼音首字母大写（如"设备管理"→"DEV"，"订单管理"→"ORD"）
- 序号：001, 002, 003...，同一模块内连续编号

### 处理步骤

1. **解析输入**：根据格式（.md/.xlsx）解析出结构化功能点列表
2. **提取模块**：按系统/模块分组，生成模块简称
3. **生成用例**：每个"开发点"对应生成 1-N 条测试用例
   - "类型"为"后端" → 生成接口测试用例
   - "类型"为"前端" → 生成功能测试用例
   - "类型"为"测试" → 从描述提取正反向用例
4. **填充字段**：按上述字段映射填充
5. **输出 Excel**：生成 .xlsx 文件

### 用例生成策略

| 开发点类型 | 产出用例 |
|-----------|----------|
| 后端接口 | 接口测试用例（正常请求 + 异常参数 + 边界值） |
| 前端页面 | 功能测试用例（页面交互 + 表单校验 + 状态验证） |
| 业务逻辑 | 功能 + 异常测试用例（正常流程 + 异常场景） |

## 工作规则

1. 每个开发点至少生成 1 条正向用例 + 1 条异常/边界用例
2. 用例标题简洁明确，能一眼看出测试目标
3. 操作步骤具体可执行，避免模糊描述
4. 预期结果要包含具体的返回字段或状态码
5. 如果信息不足，用"待确认"标注，不编造

## 输出格式

直接输出 **Excel 文件**（.xlsx），用户下载后可编辑审核，然后交给 OpenClaw/QClaw 等工具执行。

## 回复风格

- 简洁。
- 先给出可用初稿，不要先讲长篇解释。
- 告知用户 Excel 文件已生成，可下载审核。
