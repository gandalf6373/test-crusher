---
name: test-crusher
description: "当你拿到已整理好的开发功能点列表（.md 或 .xlsx 格式），需要先校验输入完整度，再生成测试用例 Excel 时使用。"
---

# Test Crusher

当用户给你的是**已整理好的开发功能点列表**，希望生成可直接审核、执行的测试用例 Excel 时，使用这个 skill。

这个 skill 的核心原则不是“无脑先生成”，而是：

1. 先判断输入是否足够支撑可执行测试用例
2. 信息足够时再生成 Excel
3. 信息不足时先返回缺失项清单，不编造细节

## 支持平台

- Claude Code ✅
- Codex ✅
- OpenClaw ✅
- QClaw ✅
- openCode ✅
- 其他支持 Skill 的工具 ✅

## 输入格式

支持两种输入格式：

### 1. Markdown 格式
优先从以下标题中提取：

- `## 结构化任务（开发点）`
- `## 开发功能点`
- `## 功能点`
- `## 开发任务`
- `## 提测范围`

格式如：
```markdown
- [后端] 设备新增接口（device_no 唯一校验 / station_id 存在性校验 / 默认 AVAILABLE）
- [前端] 新增设备表单页（字段 / 必填校验 / 提交提示）
- [测试] 正常新增 / device_no 重复拦截 / station_id 无效拦截
```

### 2. Excel 格式 (.xlsx)
优先识别以下列：

- 系统
- 模块 / 功能模块
- 功能点
- 开发点 / 任务点
- 类型
- 描述 / 说明
- 优先级 / 等级
- 备注

如果列名不完全一致，但语义明确，可按最接近的字段归一化处理。

## 你要产出什么

将输入的开发功能点列表，转换为**测试用例 Excel 文件**（.xlsx 格式）。
默认以**页面操作测试**的方式输出，用点击、输入、提交、校验等步骤描述测试流程。

输出时遵循以下约束：

- 不输出 API 路径
- 不直接写接口请求参数
- 尽量落到页面入口、页面动作、页面结果
- 对只能从后端描述推断出的页面动作，必须在备注中标注“由后端描述推断”

### Excel 输出字段

| 字段 | 说明 |
|------|------|
| 用例编号 | 格式：`模块简称-序号`（如 `DEV-001`），按模块分组编号 |
| 模块 | 对应开发功能点中的"模块" |
| 用例标题 | 简洁描述测试目标 |
| 优先级 | P0（核心功能）/ P1（重要功能）/ P2（一般功能），从输入提取或推断 |
| 用例类型 | 功能测试 / 页面测试 / 边界测试 / 异常测试 |
| 前置条件 | 执行测试前需要满足的条件 |
| 执行路径 | 页面入口、菜单路径或功能入口；不输出 API 路径 |
| 输入数据 | 页面表单字段、搜索条件或可见测试数据；不写接口请求参数 |
| 操作步骤 | 详细页面操作步骤（1. 打开页面 2. 输入内容 3. 点击提交 4. 校验结果） |
| 预期结果 | 期望的返回或响应 |
| 执行方式 | 手动执行 / 自动化可执行 |
| 是否可自动化 | 是 / 否 / 待定 |
| 备注 | 补充说明 |

## 处理逻辑

### 总流程

1. **识别输入**：识别 Markdown / Excel 中的结构化开发点
2. **归一化字段**：统一映射系统、模块、功能点、开发点、类型、描述、优先级、备注
3. **校验完整度**：判断是否具备生成可执行测试用例的最低信息
4. **决定是否生成**：
   - 信息足够：继续生成 Excel
   - 信息不足：先输出缺失信息清单，不直接生成 Excel
5. **生成用例**
6. **执行输出前自检**
7. **输出 Excel 或输出缺失项说明**

### 输入完整度判定

以下三项用于判断是否可以直接生成：

- `模块` 明确
- `开发点主体` 明确
- 能映射出 `页面动作` 或 `可见业务结果`

判定规则：

- 如果以上三项里缺失 **2 项或以上**，不要直接生成 Excel
- 先输出“缺失信息清单 + 建议补充格式”
- 如果只缺失 **1 项**，允许继续生成，但未知项必须写 `待确认`
- 如果输入仅有后端描述，但能推断出页面结果，可继续生成；备注中必须写明 `由后端描述推断`

### 用例编号规则

- 格式：`{模块简称}-{序号}`
- 模块简称：取模块名拼音首字母大写（如"设备管理"→"DEV"，"订单管理"→"ORD"）
- 序号：001, 002, 003...，同一模块内连续编号

### 处理步骤

1. **解析输入**：根据格式（.md/.xlsx）解析出结构化功能点列表
2. **提取模块**：按系统/模块分组，生成模块简称
3. **完整度校验**：先判断是否达到直接生成门槛
4. **生成用例**：每个“开发点”对应生成 1-N 条测试用例
   - 默认生成页面功能测试用例
   - 从输入里提取页面名称、按钮、字段、提示、校验点
   - 如果只有后端/接口描述，也要转换成页面视角的测试步骤
   - 无法确定页面入口、字段名或测试数据时写 `待确认`
5. **填充字段**：按上述字段映射填充
   - 不输出 API 路径
   - 执行步骤只写页面点击、输入、提交、校验
   - 推断项必须在备注中标注来源
6. **输出前自检**
7. **输出 Excel**：生成 .xlsx 文件

### 用例生成策略

| 开发点类型 | 产出用例 |
|-----------|----------|
| 后端接口 | 页面视角的功能测试用例，聚焦可见结果和管理入口，入口不明确时标 `待确认` |
| 前端页面 | 功能测试用例（页面交互 + 表单校验 + 状态验证） |
| 业务逻辑 | 页面视角的功能 + 异常测试用例（正常流程 + 异常场景） |

### 默认覆盖矩阵

除非用户明确要求缩减范围，否则每个开发点都应尽量检查以下维度：

- 正常流程
- 必填校验
- 格式校验
- 长度或边界值
- 唯一性校验
- 异常提示
- 状态流转
- 关联对象存在性或有效性
- 重复提交
- 列表结果可见性（新增后查询、筛选、列表状态变化）

如果某个维度明显不适用，可以不生成对应用例，但不要无理由漏掉。

## 工作规则

1. 每个开发点至少生成 1 条正向用例 + 1 条异常/边界用例
2. 用例标题简洁明确，能一眼看出测试目标
3. 操作步骤必须是页面可执行动作，避免接口语言
4. 预期结果要包含可见页面状态、提示文案或列表变化
5. 如果页面入口、字段名或测试数据不明确，用 `待确认` 标注，不编造
6. 可以推断页面动作，但必须在备注中标注 `由后端描述推断`
7. 不允许编造具体按钮名、返回文案、字段枚举值
8. 优先生成可审核、可执行、可追溯的用例，而不是表面完整的用例

## 输出格式

### 情况 A：信息足够

直接输出 **Excel 文件**（.xlsx），用户下载后可编辑审核，然后交给 OpenClaw/QClaw 等工具执行。

输出前应先用 3-5 行简洁说明告知：

- 识别到的模块数量
- 识别到的开发点数量
- 预计或实际生成的用例数量
- 存在的关键假设或待确认项

### 情况 B：信息不足

不要伪造 Excel 内容。

直接输出：

1. 缺失信息清单
2. 为什么这些信息会影响用例可执行性
3. 建议用户补充的最小示例格式

只有在达到最低完整度门槛后，才生成 Excel。

## 输出前自检

在声称“已生成可用 Excel”之前，至少检查以下项目：

1. 用例编号是否唯一
2. 每个开发点是否至少映射 2 条用例
3. 是否混入 API 路径、接口参数、纯后端术语
4. 模块、标题、步骤、预期结果是否存在明显空值
5. 所有推断项是否已在备注中标明

如果自检不通过，应先修正后再输出。

## 回复风格

- 简洁。
- 先给出结论，不要先讲长篇解释。
- 如果不能直接生成，就明确说缺什么。
- 如果已生成 Excel，告知用户文件已生成，可下载审核。
